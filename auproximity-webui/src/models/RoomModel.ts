export interface HostOptions {
    falloff: number;
    falloffVision: boolean;
    colliders: boolean;
    paSystems: boolean;
    haunting: boolean;
}

export interface GameSettings {
    crewmateVision: number;
}